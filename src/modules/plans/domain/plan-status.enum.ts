/** Commercial availability of a plan. */
export enum PlanStatus {
  Active = 'ACTIVE',
  Inactive = 'INACTIVE',
}

/** Billing cadence of a plan. */
export enum BillingPeriod {
  Monthly = 'MONTHLY',
  Yearly = 'YEARLY',
}
